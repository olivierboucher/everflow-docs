---
title: Create Channel
openapi: /openapi/channels.yaml post /networks/channels
---

Create a new channel. Channels categorize and organize offers for reporting and management.

Both `name` and `status` fields are required. Set the status to `active` or `inactive`.
