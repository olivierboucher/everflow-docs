---
title: List Labels
openapi: get /networks/labels
---

Retrieve all labels for the network. Labels can be applied to offers, affiliates, advertisers, campaigns, and offer groups to organize and filter resources.

Each label in the response includes the IDs of all resources it is applied to, making it easy to see which entities share a given label.
