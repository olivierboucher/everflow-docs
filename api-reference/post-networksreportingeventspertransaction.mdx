---
title: Get Events Per Transaction
openapi: /openapi/reporting-extras.yaml post /networks/reporting/events/pertransaction
---

Returns events grouped by transaction. Shows all post-conversion events and their details for each transaction in the specified time range. Each request requires `from`, `to`, and `timezone_id`.
